<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="board">

	<select id="selectBoard" resultType="com.javaex.vo.BoardVo">
		<![CDATA[
			select  bo.no as no
				    ,bo.title as title
					,bo.content as content
					,bo.hit as hit
					,bo.reg_date as regDate
					,us.name as name
					,us.no as userNo
			FROM board bo, users us
			where bo.user_no = us.no
			ORDER BY bo.no desc
		]]>
	</select>

	<insert id="listInsert" parameterType="com.javaex.vo.BoardVo">  
		 <![CDATA[
	     INSERT INTO guestbook 
	     VALUES (seq_no.nextval
	             ,#{name}
	             ,#{password}
	             ,#{content}
	             ,SYSDATE )
		 ]]>
	</insert>

	<delete id="listDelete" parameterType="com.javaex.vo.BoardVo">
		<![CDATA[
			delete from guestbook
			where no = #{no}
			and password = #{password}		
		]]>
	</delete>



</mapper>